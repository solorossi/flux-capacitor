# Flux Capacitor

This repository contains a ReST service that converts timestamps from one time zone to another and calculates the offset between time zones.

This service is useful for environments that don't have access to the zone info database, like older versions of Smalltalk, etc.
